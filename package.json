{
  "private": true,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "version": "0.0.0"
}
